import { createTheme } from "@mui/material";
const theme = createTheme({
  palette: {
    white: "#fff",
    black: "#000",
    liteBlack: "#424242",
    darkBlue: "#1F1754",
    textGray: "#868686",

  },
});
export default theme;
